{
  "name": "app",
  "version": "0.0.1",
  "description": "A beatiful repository",
  "main": "index.js",
  "scripts": {
    "lint": "npm run lint",
    "prettier": "npm run prettier",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^7.32.0",
    "prettier": "^3.4.2"
  }
}
